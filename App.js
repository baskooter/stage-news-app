import React, { Component, useEffect, useState } from 'react';

import { SafeAreaView, FlatList, StyleSheet, Text, View, Alert, Image, TouchableOpacity, Linking } from 'react-native';

import HomeScreen from './Component/HomePage';

export const App = props => {
	
	return (
		<>
			<HomeScreen/>
		</>
	);
}


export default App; 